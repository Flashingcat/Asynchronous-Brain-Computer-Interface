# Online inference

Use the saved checkpoint and its training-session normalisation statistics. Incoming EEG must be filtered to 8--30 Hz, resampled to 128 Hz, and supplied as 22 channels by 256 samples. Advance the buffer every 0.5 seconds.
